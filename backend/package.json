{
  "name": "ecommerce-json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx json-server db.json --host 0.0.0.0 --port 10000",
    "start:prod": "npx json-server db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
